
/**
 * Write a description of class Exersice3 here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Exersice3
{
    public void Anand (double a, double b)
    {
        double z = 0.0;
        double f = 1.0;
        if (((z >= a) || (a <= f)) && (z >= b) || (b <= f))
        {
            System.out.println(true);
        }
        if (((z <= a) || (a >= f)) && (z <= b) || (b >= f))
        {
            System.out.println(false);
        }
    }
}
